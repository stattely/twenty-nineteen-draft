# Verify v21

After deploying, confirm these are visible:

1. Footer says `v21`.
2. Top microline includes a small `v21` pill.
3. Empty role slots use the uploaded PNG icons.
4. Events not revealed say `Carta cerrada`.
5. `Sin simulación cargada` appears as a small bubble, not a giant card.
6. Hovering cards on desktop produces visible 3D tilt and light reflection.

If the site does not show `v21`, the browser or Cloudflare is still serving an older deployment.
