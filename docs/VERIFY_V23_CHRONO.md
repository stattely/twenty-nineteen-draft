# Verify v23 chrono

If the deploy is correct, you must see:
- `v23 chrono` in the top marker/footer/title.
- A `Chrono` tab.
- A `Chrono Lab` card in Draft.
- Role icons from `/assets/roles-original-v22/`.

If not, you are seeing an older build.
