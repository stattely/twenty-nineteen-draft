{
  "name": "twenty-nineteen-draft-v12-stable",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "check": "node scripts/check-html.mjs",
    "sync:leaguepedia": "node scripts/sync-leaguepedia.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.0",
    "vite": "^6.4.3",
    "wrangler": "^4.98.0"
  }
}
